POTD-#5
TASK-2-VALID ANAGRAM
APPROACH-
1.)Sort bothe strings.
2.)Compare them.
3.)If they are equal return true.
4.)Otherwise return false.
SOLUTION-
class Solution {
public:
    bool isAnagram(string s, string t) {
        sort(s.begin(),s.end());
        sort(t.begin(),t.end());
        if(s.compare(t)==0)
        return true;
        else
        return false;
}
};
